{
  "name": "pulse",
  "version": "1.0.0",
  "private": true,
  "description": "Pulse — a one-thumb neon endless arcade game (mobile-web)",
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "serve": "bun run scripts/serve.ts",
    "smoke": "node scripts/smoke.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "playwright-core": "1.49.0",
    "typescript": "^5.6.0"
  }
}
