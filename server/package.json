{
  "name": "pulse-server",
  "version": "1.0.0",
  "private": true,
  "description": "Pulse backend — guest+account auth, cloud saves, and leaderboards",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "migrate": "bun run src/db/migrate-cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.0"
  }
}
